rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 0. Global Safety Net
    match /{document=**} {
      allow read, write: if false;
    }

    // Primitive Helpers
    function isValidId(id) { 
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$'); 
    }
    function incoming() { return request.resource.data; }
    function existing() { return resource.data; }
    function isSignedIn() { 
      return request.auth != null && request.auth.token.email_verified == true; 
    }
    
    // Album Validation
    function isValidAlbum(data) {
      return data.keys().hasAll(['stickers', 'stickerNames', 'packsOpened', 'totalSpent', 'userId', 'updatedAt']) 
        && data.keys().size() == 6
        && data.stickers is map
        && data.stickerNames is map
        && data.packsOpened is number && data.packsOpened >= 0
        && data.totalSpent is number && data.totalSpent >= 0
        && data.userId == request.auth.uid
        && data.updatedAt == request.time;
    }

    // Trade Log Validation
    function isValidActivityLog(data) {
      return data.keys().hasAll(['userId', 'type', 'createdAt'])
        && data.userId == request.auth.uid
        && data.type in ['trade', 'pack', 'add', 'remove']
        && data.createdAt == request.time;
    }

    match /albums/{userId} {
      allow read: if isSignedIn() && request.auth.uid == userId;
      
      allow create: if isSignedIn() 
        && isValidId(userId)
        && request.auth.uid == userId
        && isValidAlbum(incoming());

      allow update: if isSignedIn() 
        && isValidId(userId)
        && request.auth.uid == userId
        && isValidAlbum(incoming())
        && incoming().userId == existing().userId
        && incoming().diff(existing()).affectedKeys().hasOnly(['stickers', 'stickerNames', 'packsOpened', 'totalSpent', 'updatedAt']);
      
      match /tradeLogs/{logId} {
        allow read, list: if isSignedIn() && request.auth.uid == userId;
      }

      match /activityLogs/{logId} {
        allow read, list: if isSignedIn() && request.auth.uid == userId;

        allow create: if isSignedIn()
          && isValidId(userId)
          && request.auth.uid == userId
          && isValidId(logId)
          && isValidActivityLog(incoming())
          && exists(/databases/$(database)/documents/albums/$(userId));
      }
    }
  }
}
